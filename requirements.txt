# GeoVision (Streamlit app) — Python 3.10+ recommended
# Install locally: pip install -r requirements.txt
# Streamlit Cloud reads this file from the repository root on each deploy.
streamlit>=1.33.0
openai>=1.54.0
httpx>=0.27.0
